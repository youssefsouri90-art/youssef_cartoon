pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven("https://jitpack.io")
    }
}

dependencyResolutionManagement {
    // تم تغيير هذا السطر للسماح للمشاريع بتعريف مستودعاتها الخاصة إذا لزم الأمر
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)
    repositories {
        google()
        mavenCentral()
        maven("https://jitpack.io")
    }
}

rootProject.name = "youssef_cartoon"
include(":CarateenProvider")
